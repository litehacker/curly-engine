import { defineAgent } from "eve";

export default defineAgent({
  description:
    "Frontend engineer who builds the Next.js UI for the Youtube → MIDI transcriber. Use for UI, UX, and shadcn work.",
  model: "alibaba/qwen3.8-27b",
});
