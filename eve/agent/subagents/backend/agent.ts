import { defineAgent } from "eve";

export default defineAgent({
  description:
    "Backend engineer who builds Postgres, Prisma, CRUD, and the Youtube-to-MIDI pipeline. Use for APIs, jobs, and algorithms.",
  model: "zai/glm-5.3-flash",
});
