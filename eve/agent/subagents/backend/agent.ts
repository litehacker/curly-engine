import { defineAgent } from "eve";

export default defineAgent({
  description:
    "Backend engineer who builds Python,Postgres, Prisma, CRUD, and job plumbing for Youtube → MIDI. Use for APIs and jobs — not transcription math, geometry, or numeric methods (those go to researchEngineer).",
  model: "zai/glm-5.3-flash",
});
