import { defineAgent } from "eve";

export default defineAgent({
  description:
    "DevOps engineer who owns GitHub and Vercel CI/CD. Use for Actions, deploys, env, and pipeline health — not product features.",
  model: "zai/glm-5.3-flash",
});
